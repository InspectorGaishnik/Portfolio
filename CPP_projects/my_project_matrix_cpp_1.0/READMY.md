# Matrix library C++

Статическая библиотека для работы с матрицами реализованная на языке C++.

Version - 1.0.0.

![alt text](Images/image.png)

## 📋 Оглавление

- [Features] (#Features)
- [Usage] (#Usage)

## ✨ Features

- ✅ Библиотека разработана на языке C++ стандарта C++20 с использованием компилятора gcc.
- ✅ Код отформатирован Google Style.
- ✅ Матрица реализована в виде класса Matrix
- ✅ Программа реализована как статическая библиотека с заголовочным файлом matrix_oop.h.
- ✅ Тестирование производится с помощью библиотеки GTest.
- ✅ Сборка производится с помощью Makefile.
- Цели Makefile:
▪ all
▪ matrix_oop.a
▪ test
▪ valgrind
▪ gcov_report
▪ run
▪ clang_format
▪ clean
- ✅ В проекте не используются контейнеры STL.

## 📋 Usage

### ----- Preliminary requirements

- Compiler: C++ с поддержкой стандарта C++20 или новее.
- Система сборки: Make.

### ----- installing 

#### Если необходимо клонировать только отдельный проект:
- Создаем репозиторий (при его отсутствии):  
mkdir project-folder && cd project-folder  
git init
- Включаем sparse checkout:  
git config core.sparseCheckout true
- Указываем нужную папку с проектом:  
echo "CPP_projects/my_project_matrix_cpp_1.0/" >> .git/info/sparse-checkout
- Добавляем удаленный репозиторий:  
git remote add origin git@github.com:InspectorGaishnik/Portfolio.git
- Скачиваем только указанную папку:  
git pull origin main
- Перейти в папку нужного проекта:  
cd CPP_projects/my_project_matrix_cpp_1.0/src/
- Запустить Make, для компиляции статической библиотеки:  
make -> matrix_oop.a

#### Если необходимо клонировать весь репозиторий:
- Клонируем репозиторий:  
git clone "git@github.com:InspectorGaishnik/Portfolio.git".
- Перейти в папку нужного проекта:  
cd CPP_projects/my_project_matrix_cpp_1.0/src/
- Запустить Make, для компиляции статической библиотеки:  
make -> matrix_oop.a

### ----- launch

- Для использования необходимо создать main.cpp, объявить в нем matrix_oop.h как заголовочный файл и при компиляции исполняемого файла, подключать библиотеку:  
Пример команды компиляции:  
gcc -Wall -Werror -Wextra main.cpp -o main.out matrix_oop.a -lstdc++

### ----- Testing

- Перейти в папку с тестовыми файлами:  
cd project-name/src/
- Компиляция исполняемых файлов и их исполнение  
make test
