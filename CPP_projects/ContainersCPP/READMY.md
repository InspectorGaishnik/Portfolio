# Decimal library C

Реализация основных стандартных контейнерных классов языка C++.
Array, list, vector, stack, queue, map, set, multiset.
Реализации представляет весь набор стандартных методов и атрибутов для работы с элементами, проверкой заполненности контейнера и итерирования.

Version - 1.0.0.

![alt text](Images/image.png)

## 📋 Оглавление

- [Features] (#Features)
- [Features] (#Containers)

## ✨ Features

- ✅ Библиотека разработана на языке C++ стандарта C++20 с использованием компилятора gcc.
- ✅ Код отформатирован Google Style.
- ✅ Классы реализованы внутри пространства имен dn.
- ✅ Соблюдена логика работы стандартной библиотеки шаблонов (STL) в части проверок, работы с памятью и поведения в нештатных ситуациях.

## 📋 Usage

### ----- Preliminary requirements

- Compiler: C++ с поддержкой стандарта C++20 или новее.
- Система сборки: Make.

### ----- installing 

#### Если необходимо клонировать только отдельный проект:
- Создаем репозиторий (при его отсутствии):  
mkdir project-folder && cd project-folder  
git init
- Включаем sparse checkout:  
git config core.sparseCheckout true
- Указываем нужную папку с проектом:  
echo "CPP_projects/ContainersCPP/" >> .git/info/sparse-checkout
- Добавляем удаленный репозиторий:  
git remote add origin git@github.com:InspectorGaishnik/Portfolio.git
- Скачиваем только указанную папку:  
git pull origin main
- Перейти в папку нужного проекта:  
cd CPP_projects/ContainersCPP/src/

#### Если необходимо клонировать весь репозиторий:
- Клонируем репозиторий:  
git clone "git@github.com:InspectorGaishnik/Portfolio.git".
- Перейти в папку нужного проекта:  
cd CPP_projects/ContainersCPP/src/

### ----- launch

- Для использования необходимо создать main.cpp, объявить в нем dn_containers.h как заголовочный файл:
Пример команды компиляции:  
gcc -Wall -Werror -Wextra main.cpp -o main.out -lstdc++

### ----- Testing

- Перейти в директорию src:  
cd project-folder/CPP_projects/ContainersCPP/src/
- Компиляция исполняемых файлов и их исполнение:   
make all
